run with the syntax:

python carletonian_upload.py carletonianLocation saveLocation

where carletonianLocation is the directory that the carletonian files are located in, and saveLocation is where you want the import files to go.

a new folder will be created in saveLocation to hold the save files.